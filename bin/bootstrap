#!/bin/sh
set -eu

cd "$(dirname "$0")/.."

# Ensure cosmic-lua is available and install the skill
./bin/cosmic --skill >/dev/null 2>&1 || true

# Create symlink if it doesn't exist
[ -L bin/lua ] || ln -sf cosmic-lua bin/lua
